/**
 * Hostname of Raspberry Pi streaming server.
 */
export const ip =  'challah.local';

export const viewerIp =  'sourdough.local';

/**
 * Offset in ms of when heartbeat occured and when the pi detected it
 */
export const beatoffset = -75